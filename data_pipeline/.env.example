# Copy to .env and fill in. NEVER commit .env (see AGENT_WORK_ORDER §0.6).

# --- Postgres (data pipeline ingest target, Phase 11) ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=englow3
PGUSER=postgres
PGPASSWORD=

# --- LLM provider (Phase 5+) ---
ANTHROPIC_API_KEY=

# --- Embedding provider (Phase 11) — model/dimension chốt tại schemas/embedding_config.yaml ---
EMBEDDING_API_KEY=
