package com.englow3.exam.entity;

/** CEFR band a paper is aimed at. {@code exams.target_level} is a plain varchar(2), so this enum is what guards it. */
public enum TargetLevel {
    A1, A2, B1, B2, C1, C2
}
