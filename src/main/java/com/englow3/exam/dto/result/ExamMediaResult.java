package com.englow3.exam.dto.result;

/** What {@code AdminExamService.uploadMedia} hands back - a key to place into the content tree, nothing else. */
public record ExamMediaResult(String objectKey) {
}
