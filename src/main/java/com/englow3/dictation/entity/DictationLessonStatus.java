package com.englow3.dictation.entity;

/** Same lifecycle as {@link FlashcardSetStatus}, and a separate enum for the same reason. */
public enum DictationLessonStatus {
    DRAFT, PENDING_REVIEW, REJECTED, PUBLISHED, ARCHIVED
}
