target/
.idea/
*.iml
.vscode/
.mvn/wrapper/maven-wrapper.jar
HELP.md

# --- data_pipeline ---
data_pipeline/.venv/
data_pipeline/**/__pycache__/
data_pipeline/.pytest_cache/
.env

# Wordlist gốc tải về — bên thứ ba, không commit vào repo
data_pipeline/seeds/raw/

# --- Dữ liệu học liệu: CHỈ LƯU LOCAL, không đẩy lên git ---
# Đề thi, flashcard, quiz, seed từ vựng — sinh lại bằng script trong generators/
data_pipeline/output/**/*.json
data_pipeline/seeds/vocab_seed.csv
data_pipeline/seeds/by_level/
data_pipeline/rejects/*.json
data_pipeline/output/_db/*.jsonl
data_pipeline/output/_db/_manifest.json

# DDL sinh tu dong — sinh lai bang `make schema`
data_pipeline/migrations/
